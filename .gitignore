build/*.rbxl
build/*.rbxlx
build/*.rbxm
*.rbxl.lock
.superpowers/
.tmp/

# Weppy Studio sync plugin runtime (Rojo is the source of truth here)
weppy-project-sync/
build/*.png

# generated by tools/stamp.py (git hash + stamp time shown in the HUD build pill)
src/shared/Build.luau

# Local build output and scratch dumps (regenerated by Rojo / tools/world_harness.luau)
build/
/build.rbxlx
/map.json
__pycache__/

# Blender autosave backups (duplicates of the .blend next to them)
*.blend1

# Per-agent session state, not project source
.claude/
.opencode/
CC-Session-Logs/
